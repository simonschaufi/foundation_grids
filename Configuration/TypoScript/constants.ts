## Define custom categories
# customsubcategory=settings=Settings

plugin.tx_foundationgrids {
	settings {
		# cat=plugin.tx_foundationgrids/settings/01; type=options[5,6]; label= foundation version
		foundationVersion = 6
	}
}